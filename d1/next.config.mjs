/** @type {import('next').NextConfig} */
const nextConfig = {
    trailingSlash: true,
    assetPrefix: '',  // Use a leading slash for asset prefix
    output: 'export',
  };
  
  export default nextConfig;
  